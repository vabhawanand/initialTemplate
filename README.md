# initialTemplate


download the repository

rename root folder to project name

cd <project-name>
npm i && 
npm audit fix --force && 
npx eject && 
npx react-native rename "New AppName" && 
npx pod-install ios

